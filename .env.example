# =========================================================
# Zenbat — Variables d'environnement (exemple)
# Copiez ce fichier en « .env.local » pour le dev local
# Ne JAMAIS commiter le vrai .env
# =========================================================

# Clé API Anthropic (obligatoire) — utilisée côté serveur par /api/claude.js
# À obtenir sur https://console.anthropic.com/
# ⚠️ Jamais exposée au navigateur : pas de préfixe VITE_
ANTHROPIC_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# Modèle Claude par défaut (optionnel)
# CLAUDE_MODEL=claude-sonnet-4-5

# Origines autorisées en production (CSV, sans espace)
# Utilisé par api/claude.js pour durcir le CORS
# En preview/dev, laisser vide : l'origin de la requête sera renvoyé.
# ALLOWED_ORIGINS=https://zenbat.fr,https://www.zenbat.fr

# =========================================================
# Supabase (obligatoire)
# Supabase Dashboard → Project Settings → API
# =========================================================
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# =========================================================
# Odoo Sign (signature électronique — api/odoo-sign.js)
# Côté serveur uniquement (pas de préfixe VITE_)
# Génère une API key dans Odoo → Préférences → Compte → Clés API
# =========================================================
ODOO_URL=https://zenbat.odoo.com
ODOO_DB=zenbat
ODOO_USERNAME=admin@zenbat.fr
ODOO_API_KEY=xxxxxxxxxxxxxxxx

# =========================================================
# Panel Admin (api/admin-stats.js)
# Supabase → Project Settings → API → service_role key (secret)
# ⚠️ JAMAIS exposée au navigateur (pas de préfixe VITE_)
# =========================================================
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...service_role...

# Email de l'administrateur Zenbat (votre email de connexion)
# Côté serveur : protège l'endpoint /api/admin-stats
ADMIN_EMAIL=votre@email.com

# Côté client : affiche le bouton "Admin" dans le header
# Doit correspondre exactement à l'email ci-dessus
VITE_ADMIN_EMAIL=votre@email.com
